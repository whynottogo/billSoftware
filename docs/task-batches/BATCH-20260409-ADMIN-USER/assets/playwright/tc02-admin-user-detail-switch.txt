### Result
{"beforeTab":"月账单","afterTab":"年账单","beforeYear":"2026","afterYear":"2025"}
### Ran Playwright code
```js
await (async (page) => { const beforeTab = await page.locator('.tab-btn.is-active').innerText(); await page.locator('.tab-btn').nth(1).click(); await page.waitForTimeout(200); const afterTab = await page.locator('.tab-btn.is-active').innerText(); const beforeYear = await page.locator('.admin-user-detail-page__filter-row select').first().inputValue(); await page.locator('.admin-user-detail-page__filter-row select').first().selectOption({ index: 1 }); await page.waitForTimeout(150); const afterYear = await page.locator('.admin-user-detail-page__filter-row select').first().inputValue(); return { beforeTab, afterTab, beforeYear, afterYear }; })(page);
```
